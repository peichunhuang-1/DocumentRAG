export interface PdfProps {
    src: string;
    source: string;
}

export interface UnstructuredClientProps {
    serverURL: string;
}

export interface MessageProps {
    user: string;
    content: string;
}